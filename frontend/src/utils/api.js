// Central API configuration
// In development (localhost), use localhost:8000
// In production (Vercel), use the Render backend URL
// In development and production, use the Render backend URL for total synchronization
export const API_BASE_URL = `https://rosa-elena-store.onrender.com`;
